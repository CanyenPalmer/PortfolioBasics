// (Optional hook reserved for future per-card scroll progress needs.)
// For now, the main Experience component uses viewport-center distance to pick focus.
// Keeping this file so you have a stable place if you want to evolve to per-card progress later.

export {};
